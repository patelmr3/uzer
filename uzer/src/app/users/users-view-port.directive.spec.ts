import { UsersViewPortDirective } from './users-view-port.directive';

describe('UsersViewPortDirective', () => {
  it('should create an instance', () => {
    const directive = new UsersViewPortDirective();
    expect(directive).toBeTruthy();
  });
});
