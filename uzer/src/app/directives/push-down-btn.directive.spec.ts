import { PushDownBtnDirective } from './push-down-btn.directive';

describe('PushDownBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new PushDownBtnDirective();
    expect(directive).toBeTruthy();
  });
});
